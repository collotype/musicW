<UserControl x:Class="Nocturne.App.Controls.TrackRowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Button Command="{Binding PlayCommand, ElementName=Root}"
            CommandParameter="{Binding Track, ElementName=Root}"
            Background="Transparent"
            BorderThickness="0"
            Padding="0"
            HorizontalContentAlignment="Stretch">
        <Border Background="{StaticResource Brush.PanelBackground}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="14"
                Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="72" />
                </Grid.ColumnDefinitions>
                <Border Width="42"
                        Height="42"
                        CornerRadius="12"
                        ClipToBounds="True">
                    <Image Source="{Binding Track.CoverArtUrl, ElementName=Root}"
                           Stretch="UniformToFill" />
                </Border>
                <StackPanel Grid.Column="1"
                            Margin="14,0,0,0">
                    <TextBlock Text="{Binding Track.Title, ElementName=Root}"
                               Foreground="{StaticResource Brush.TextPrimary}"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Margin="0,4,0,0"
                               Style="{StaticResource Text.Caption}"
                               Text="{Binding Track.ArtistName, ElementName=Root}" />
                </StackPanel>
                <TextBlock Grid.Column="2"
                           VerticalAlignment="Center"
                           Style="{StaticResource Text.Caption}"
                           Text="{Binding Track.Source, ElementName=Root}" />
                <TextBlock Grid.Column="3"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Style="{StaticResource Text.Caption}"
                           Text="{Binding Track.DisplayDuration, ElementName=Root}" />
            </Grid>
        </Border>
    </Button>
</UserControl>
